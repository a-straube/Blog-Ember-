<h1>About</h1>

<button class="btn btn-primary" {{action 'buttonAction'}}>Click here</button>

{{#if isPictureShowing}}
<button class="btn btn-primary" {{action 'hidePicture'}}>Hide picture</button>
<p><img src="/img/chicken.JPG"></p>
{{else}}
<button class="btn btn-primary" {{action 'showPicture'}}>Click here to see a picture</button>
{{/if}}